services:
  nginx-proxy:
    image: nginxproxy/nginx-proxy:1.11.6
    container_name: nginx-proxy
    ports:
      - "80:80"
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
      - ./alb-healthz.conf:/etc/nginx/conf.d/alb-healthz.conf:ro
